<% provide(:title, @user.name) %>
      <h1>
        <%= @user.name %>
      </h1>

 <% if @user.events.any? %>
  <h3>Events (<%= @user.events.count %>)</h3>
  <ol class="events">
    <%= render @events %>
  </ol>
<% end %>
